#include "my_functions.h"

/*
 * This function takes an int in parameter and prints it.
 * It uses the standard lib (printf).
 */
void printf_number(int n)
{
    printf("%d", n); /* Prints integers n as assigned  */
}
